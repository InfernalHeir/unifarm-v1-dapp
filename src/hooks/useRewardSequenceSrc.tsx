import { SupportedTokens } from "../constants";
import { useSelectedTokens } from "../store/stake/hooks";

const useRewardSequenceSrc = () => {
  const selectedStakeToken = useSelectedTokens();
};

export {};
